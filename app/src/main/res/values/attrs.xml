<resources>

    <!-- Declare custom theme attributes that allow changing which styles are
         used for button bars depending on the API level.
         ?android:attr/buttonBarStyle is new as of API 11 so this is
         necessary to support previous API levels. -->
    <declare-styleable name="ButtonBarContainerTheme">
        <attr name="metaButtonBarStyle" format="reference" />
        <attr name="metaButtonBarButtonStyle" format="reference" />

        <!--Text colour-->
        <attr name="attrTextColor" format="color"/>
        <attr name="attrTextSecondaryColor" format="color"/>
        <attr name="attrNewIndicator" format="color"/>
        <!--Background colour-->
        <attr name="attrBackgroundColor" format="color"/>
        <attr name="attrOutlineColor" format="color"/>
        <!--Button colour-->
        <attr name="attrButtonColor" format="color"/>
        <attr name="attrButtonTextColor" format="color"/>
    </declare-styleable>

</resources>
